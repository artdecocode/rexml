```### extractTagsSpec => {content, props}[]
[
  ["tag", "string"],
  ["string", "string"]
]
```

Same as the default method, but confirms to the XML specification in defining attributes.

%EXAMPLE: example/spec, ../src => rexml, javascript%
%FORK-json example/spec%

%~%